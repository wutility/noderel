#!/usr/bin/env node

const argv = require('minimist')(process.argv.slice(2));
const noderel = require('../src/noderel')

noderel({
  entry: argv.entry,
  watch: argv.watch ? argv.watch.split(',') : argv.watch,
  ignore: argv.ignore ? argv.ignore.split(',') : argv.ignore
});